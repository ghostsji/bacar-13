import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bacar.mqttapp',
  appName: 'BACAR MQTT App',
  webDir: 'dist/MQTTAndroidApp/browser'
};

export default config;
